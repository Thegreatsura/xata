apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: gateway-configmap-reader
rules:
  - apiGroups:
      - ""
    resources:
      - configmaps
    resourceNames:
      - ipfiltering
    verbs:
      - get
      - list
      - watch

