apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ksm.yaml

configMapGenerator:
  - name: kube-state-metrics-values
    namespace: xata
    options:
      disableNameSuffixHash: true
    files:
      - values.yaml
