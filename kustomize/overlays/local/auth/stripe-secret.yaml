apiVersion: v1
kind: Secret
metadata:
  name: stripe-secret
  namespace: xata
type: Opaque
stringData:
  # Add a testing API Key here for local testing
  api-key: ""
  stripe-webhook-secret: "secret"
